{% macro textarea(id, name='', label='', placeholder='', disabled=false, content='') %}
  {% if label %}
    <label for="{{ id }}">{{ label }}</label>
  {% endif %}
  <textarea id="{{ id }}" name="{{ name }}" placeholder="{{ placeholder }}"
    {% if disabled %}disabled{% endif %}>{{ content }}</textarea>
{% endmacro %}
